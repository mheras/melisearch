<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mercadolibre.melisearch">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <!-- Services -->

        <service
            android:name=".service.MeliSearchSpiceService"
            android:exported="false" />

        <!-- Activities -->

        <activity
            android:name=".activity.concrete.SearchActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.concrete.SearchResultsActivity"
            android:label="@string/title_activity_search_results" />

        <activity
            android:name=".activity.concrete.DetailsActivity"
            android:label="@string/title_activity_details" />

    </application>

</manifest>
